@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personal Data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>Su cuenta contiene información personal que nos facilito. Por ello, en esta página puede descargar o eliminar dicha información</p>
        <p>
            <strong>Al borrar su cuenta se elimina de forma permanente y no puede ser recuperada.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post">
            <button class ="btn bg-primary px-4 py-2 mb-4 text-dark" type="submit">Descargar</button>
        </form>
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn bg-secondary px-4 py-2 mb-4 text-light">Borrar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
